# گزارش بررسی و تحلیل فنی پروژه Hejleh (hejleh-app)

## ۱. معرفی پروژه
برنامه **Hejleh** یک سامانه پیشرفته و جامع مدیریت و برنامه‌ریزی شیفت‌های کاری (Rostering) پرسنل بیمارستان/کلینیک است که با استفاده از هوش مصنوعی (Gemini API) به صورت هوشمند و خودکار شیفت‌ها را برنامه‌ریزی می‌کند. این برنامه به تقویم شمسی (جلالی) مجهز بوده و دارای قابلیت‌های غنی مدیریت پرسنل، بخش‌ها، قوانین، مرخصی‌ها و سیستم‌های سخنگو است.

---

## ۲. معماری سیستم و پشته فناوری (Tech Stack)

### الف) فرانت‌اند (Frontend)
- **React (v19)**: برای کتابخانه اصلی رابط کاربری.
- **Tailwind CSS**: به عنوان فریم‌ورک استایل‌دهی (بارگذاری شده از طریق CDN در [index.html](file:///c:/Users/Team/Desktop/hejle/index.html) و کاستومایز شده در [index.css](file:///c:/Users/Team/Desktop/hejle/index.css)).
- **Vite (v6)**: ابزار ساخت و توسعه سریع فرانت‌اند ([vite.config.ts](file:///c:/Users/Team/Desktop/hejle/vite.config.ts)).
- **پارسرهای فایل**:
  - **SheetJS (xlsx)** برای تحلیل فایل‌های اکسل و ورود/خروج داده‌های پرسنل.
  - **docx** برای خروجی گرفتن اطلاعات برنامه‌ریزی در قالب فایل‌های ورد (Word).
  - **FileSaver.js** برای ذخیره‌سازی فایل‌های تولید شده در سمت کلاینت.

### ب) بک‌اند (Backend)
- **Node.js + Express (v5)**: سرور میانی برای اجرای منطق برنامه و فرانت‌اند ([server.ts](file:///c:/Users/Team/Desktop/hejle/server.ts)).
- **tsx**: برای اجرای مستقیم فایل‌های TypeScript بدون نیاز به کامپایل دستی زمان توسعه.
- **esbuild**: برای باندل کردن کد سرور جهت آماده‌سازی محیط تولید.

### ج) پایگاه داده و احراز هویت
- **Firebase Auth (Google Sign-In)**: سیستم احراز هویت کاربران ابری.
- **Firebase Cloud Firestore**: برای ذخیره‌سازی ابری امن و همگام‌سازی لحظه‌ای تمامی داده‌ها (پرسنل، بخش‌ها، شیفت‌ها، قوانین و...) ([firebase.ts](file:///c:/Users/Team/Desktop/hejle/services/firebase.ts)).
- **LocalStorage**: به عنوان سیستم ذخیره‌سازی محلی پشتیبان که داده‌ها را حتی در حالت آفلاین حفظ می‌کند.

### د) هوش مصنوعی (AI Integration)
- **Google GenAI SDK (`@google/genai`)**: جهت ارتباط مستقیم با مدل‌های پیشرفته هوش مصنوعی گوگل.
- **مدل اصلی**: `gemini-3.5-flash` جهت حل مسائل پیچیده شیفت‌بندی ریاضی و لجستیکی.
- **مدل جایگزین (Fallback)**: `gemini-3.1-flash-lite` جهت مدیریت بهینه سهمیه رایگان (Quota Limit) و جلوگیری از خطاهای ۴۲۹ (تعداد درخواست زیاد).

---

## ۳. تحلیل بخش‌های اصلی برنامه (Core Components)

### ۱. برنامه‌ریز شیفت (Scheduler)
مهم‌ترین و پیچیده‌ترین جزء برنامه است که در [Scheduler.tsx](file:///c:/Users/Team/Desktop/hejle/components/Scheduler.tsx) قرار دارد:
- یک جدول تعاملی (Grid) برای نمایش شیفت‌های ماهانه پرسنل بر اساس سال و ماه جلالی ارائه می‌دهد.
- امکان ویرایش دستی شیفت‌ها، قفل کردن شیفت‌های دستی (Manual Locked Entries) و نمایش تداخل‌ها را فراهم می‌کند.
- آمارهای زمان موظفی پرسنل را به صورت زنده محاسبه کرده و کمبود یا اضافه کار را نشان می‌دهد.

### ۲. سیستم تشخیص تداخل (Conflict Detector)
ماژول [conflictDetector.ts](file:///c:/Users/Team/Desktop/hejle/services/conflictDetector.ts) وظیفه بررسی هوشمند قوانین در زمان واقعی را دارد:
- **ریکاوری شب‌کاری**: تضمین می‌کند هر فرد پس از شیفت شب (`N`) حداقل ۲۴ ساعت استراحت کند (شیفت روز بعد باید `OFF` یا `-` باشد).
- **حداکثر شب‌کاری متوالی**: محدودیت تعداد شب‌کاری‌های متوالی را (پیش‌فرض ۲ شب) بررسی می‌کند.
- **توالی عصر-صبح**: هشداری برای جلوگیری از شیفت خسته‌کننده صبح دقیقا بعد از عصر روز قبل.
- **قانون سرپرستار (Head Nurse)**: سرپرستارها را ملزم به حضور در شیفت صبح در روزهای عادی و آف بودن در روزهای جمعه و تعطیلات رسمی می‌کند.
- **مغایرت با مرخصی درخواستی**: انطباق شیفت‌ها با مرخصی‌های درخواستی پرسنل.

### ۳. مدیریت پرسنل (Personnel Manager)
مدیریت اطلاعات هویتی و شغلی پرسنل در [PersonnelManager.tsx](file:///c:/Users/Team/Desktop/hejle/components/PersonnelManager.tsx) تعریف شده است:
- نام، کد پرسنلی، نقش (سرپرستار، پرستار، بی‌هوشی و...)، بخش مربوطه، سطح سابقه، کد ملی، شماره تماس، آدرس و نوع استخدام (رسمی، قراردادی، طرحی، شرکتی).
- محاسبه خودکار کسر موظفی بر اساس سابقه کار (Seniority Level) و قوانین تعریف شده.

### ۴. مدیریت قوانین (Rules Manager)
مدیریت و فعال/غیرفعال کردن قوانین مختلف لجستیکی بیمارستان با قابلیت تعیین اولویت (بحرانی، مهم، پیشنهادی) و طبقه‌بندی (قانون کار، ایمنی بیمار، رفاه پرسنل، عدالت) در [RulesManager.tsx](file:///c:/Users/Team/Desktop/hejle/components/RulesManager.tsx).

### ۵. دستیار صوتی و متنی هوش مصنوعی (AI & Voice Assistants)
- **چت‌بات لجستیک Hejleh**: پاسخی هوشمند به سوالات کاربران درباره شیفت‌ها، قوانین و پرسنل به زبان فارسی در [AiAssistant.tsx](file:///c:/Users/Team/Desktop/hejle/components/AiAssistant.tsx).
- **دستیار صوتی زنده (Live Voice Assistant)**: امکان تعامل صوتی مستقیم و اجرای دستورات از طریق شبیه‌سازی سیستم صوتی در [LiveVoiceAssistant.tsx](file:///c:/Users/Team/Desktop/hejle/components/LiveVoiceAssistant.tsx).

### ۶. تقویم و یادآورها (Calendar & Reminders)
تقویم جلالی با قابلیت تعریف رویدادها (جلسه، یادآور، ددلاین) و سیستم خودکار اعلان سررسید زمان یادآورها بر اساس تنظیمات زمان هشدار زودهنگام (Lead Time) در [CalendarManager.tsx](file:///c:/Users/Team/Desktop/hejle/components/CalendarManager.tsx).

---

## ۴. جریان داده و همگام‌سازی (Data Flow & Synchronization)

برنامه Hejleh از یک سیستم دوگانه برای ذخیره داده‌ها استفاده می‌کند (مطابق پیاده‌سازی در [App.tsx](file:///c:/Users/Team/Desktop/hejle/App.tsx)):
1. **کلاینت (Client-Side)**: به محض تغییر هر متغیری (دپارتمان، پرسنل، شیفت‌ها و...)، تغییرات بلافاصله در `localStorage` مرورگر ذخیره می‌شوند تا از اتلاف داده‌ها جلوگیری شود.
2. **ابر (Cloud-Side)**: در صورت لاگین بودن کاربر از طریق اکانت گوگل، یک پروسه Debounce شده (با تاخیر ۳ ثانیه‌ای جهت بهینه‌سازی تعداد نوشتن‌ها در دیتابیس) فعال شده و تمامی مجموعه‌های محلی را در پایگاه داده Firebase Cloud Firestore همگام‌سازی و آینه‌سازی (Mirroring) می‌کند.

---

## ۵. تحلیل الگوریتم هوش مصنوعی شیفت‌بندی (AI Shift Solver)

الگوریتم حل مسئله شیفت‌بندی در سرور ([server.ts](file:///c:/Users/Team/Desktop/hejle/server.ts)) از مدل‌های زبانی بزرگ به عنوان موتور حل استفاده می‌کند. این کار با روش‌های زیر انجام می‌شود:
- **فرستادن دقیق داده‌ها**: تبدیل شناسه‌های پایگاه داده به ایندکس‌های عددی (`0` تا `N-1`) برای کاهش حجم توکن‌های ارسالی به مدل و بهبود دقت عددی هوش مصنوعی.
- **قوانین صریح (System Instruction)**: مشخص کردن قوانین کار بیمارستان به عنوان محدودیت‌های ریاضی صلب.
- **ساختار خروجی اجباری (JSON Schema)**: تضمین اینکه خروجی دریافتی از هوش مصنوعی همیشه ساختار معتبری از آرایه تخصیص شیفت‌ها به پرسنل و روزها دارد.
- **مکانیسم جبران خطای خودکار (Quota Fallback)**: در صورت بروز خطای خستگی سهمیه یا لیمیت نرخ تراکنش (Rate Limit 429) روی مدل `gemini-3.5-flash`، سرور به طور خودکار درخواست را روی مدل سبک‌تر و مجزای `gemini-3.1-flash-lite` ارسال می‌کند تا وقفه در کارایی سیستم ایجاد نشود.

---

## ۶. بررسی نقاط قوت (Strengths)
1. **رابط کاربری پیشرفته و مدرن**: استفاده از زبان طراحی هماهنگ با انیمیشن‌های پویا، تم‌های رنگی جذاب و قابلیت تاشو بودن سایدبار.
2. **بومی‌سازی کامل**: استفاده از تقویم جلالی، فونت صمیمانه Vazirmatn، زبان فارسی و تبدیل دقیق تاریخ‌های شمسی به میلادی برای محاسبات سیستمی ([utils.ts](file:///c:/Users/Team/Desktop/hejle/utils.ts)).
3. **دقت بالا در تشخیص تداخل‌ها**: موتور تشخیص تعارض محلی کارآمد قبل از ذخیره یا تایید برنامه.
4. **تلفیق نوآورانه هوش مصنوعی**: استفاده از قابلیت‌های استدلال منطقی LLMها برای حل فرمول‌های زمان‌بندی پیچیده نوبت کاری و دستیار صوتی زنده.
5. **پشتیبانی کامل آفلاین و آنلاین**: سیستم ذخیره‌سازی ترکیبی LocalStorage و Firestore.

---

## ۷. پیشنهادات برای بهبود سیستم (Recommendations & Roadmap)
1. **اضافه کردن موتور حل بهینه محلی (Optional Solver)**: در کنار هوش مصنوعی، اضافه کردن یک الگوریتم مبتنی بر قانون (مانند شیفت‌بندی با الگوریتم‌های ژنتیک یا محدودیت ارضا - CSP) برای زمانی که اینترنت کاملاً قطع است یا دسترسی به سرور هوش مصنوعی وجود ندارد.
2. **بهینه‌سازی فایل قوانین Firestore**: بازنگری در [firestore.rules](file:///c:/Users/Team/Desktop/hejle/firestore.rules) برای اطمینان از اینکه هر کاربر فقط به بخش دایرکتوری `users/{userId}` خود دسترسی خواندن و نوشتن دارد.
3. **یکپارچه‌سازی کامل تست‌ها**: افزودن تست‌های واحد (Unit Tests) برای توابعی مانند `jalaliToGregorian` و `detectConflicts` جهت تضمین عدم بروز خطا در آپدیت‌های بعدی.
4. **بهینه‌سازی مصرف حجم بارگذاری ابری**: استفاده از Firestore Transactions یا Write Batches اختصاصی‌تر به جای بازنویسی کامل لیست‌ها در فرآیند آینه‌سازی خودکار سه‌ثانیه‌ای، جهت کاهش هزینه‌های دیتابیس در مقیاس‌های بزرگ.

---
*تهیه شده توسط: دستیار هوش مصنوعی Antigravity*
